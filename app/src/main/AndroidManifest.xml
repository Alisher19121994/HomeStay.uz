<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.best.homestayuz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity
            android:name=".Activities.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.FragmentPage" />
        <activity android:name=".Activities.SecondRentPage" />
        <activity android:name=".Activities.AddsPage" />
        <activity android:name=".Activities.RentPage" />
        <activity android:name=".Activities.SignUp" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Activities.SignIn" />


    </application>

</manifest>